# Claude Code Action 迁移到 Gitea 的完整复刻文档

> 目标：在 **不修改原仓库** 的前提下，完整复刻到 Gitea，并完成 GitHub 专属逻辑的替换。本文档可作为迁移实施的“唯一参考”。
> 路径说明：本文统一使用 `<REPO_ROOT>` 表示当前项目根目录。当前路径为 `D:\hopesoft-hooxing\ai\claude-code-action\claude_in_gitea`。

---

**1. 项目结构与职责总览**

核心入口与职责：

- `<REPO_ROOT>\action.yml`
  入口定义与环境变量注入，强绑定 GitHub Actions。必须改写为 Gitea Actions/act_runner 的输入输出方式。
- `<REPO_ROOT>\src\entrypoints\run.ts`
  统一入口，负责：
  1) 解析上下文、鉴权、触发判断  
  2) prepare（tag/agent）  
  3) 安装并运行 Claude Code  
  4) cleanup（更新 comment + 写 summary）
- `<REPO_ROOT>\src\github\*`
  GitHub 专属上下文、API、GraphQL、权限、评论、分支、token 逻辑。必须整体替换为 Gitea 版。
- `<REPO_ROOT>\src\mcp\github-*.ts`
  GitHub MCP server，需改为 Gitea 版本并替换所有 API。
- `<REPO_ROOT>\src\create-prompt\index.ts`
  Prompt 强绑定 GitHub 语境与工具名，必须改为 Gitea 语境。
- `<REPO_ROOT>\base-action\*`
  Claude Code CLI runner，逻辑相对平台无关，建议尽量保留。

---

**2. 核心依赖与类型替换清单 (Dependency & Types)**

**2.1 需要卸载的 GitHub 专属包**

来自 `<REPO_ROOT>\package.json`：

- `@actions/github`
- `@actions/core`
- `@octokit/rest`
- `@octokit/graphql`
- `@octokit/webhooks-types`

状态：新仓库 `<REPO_ROOT>\package.json` 已完成移除上述依赖。

**2.2 推荐用于 Gitea 的替代包**

- `gitea-js`  
  如果不使用官方 SDK，也可以用 `fetch` 封装。
- 保留 `zod`、`node-fetch`（或直接用 Bun `fetch`）即可。

**2.3 全局类型替换（建议新增 `src/gitea/types.ts`）**

需要替换的 GitHub 类型与 Gitea 对应字段差异：

- `GitHubAuthor` -> `GiteaUser`
  - GitHub: `login`, `name`
  - Gitea: `login`, `full_name`

- `GitHubComment` -> `GiteaComment`
  - GitHub: `id`, `databaseId`, `body`, `author`, `createdAt`, `updatedAt`, `lastEditedAt`, `isMinimized`
  - Gitea: `id`, `body`, `user`, `created_at`, `updated_at`
  - 无 `databaseId`、`node_id`、`isMinimized`

- `GitHubReviewComment` -> `GiteaPullReviewComment`
  - GitHub: `path`, `line`, `startLine`, `side`
  - Gitea: `path`, `position`（diff position）

- `GitHubCommit` -> `GiteaCommit`
  - GitHub: `oid`, `author`
  - Gitea: `sha`, `commit.message`, `commit.author.name/email`

- `GitHubFile` -> `GiteaChangedFile`
  - GitHub: `path`, `additions`, `deletions`, `changeType`
  - Gitea: `filename`, `additions`, `deletions`, `status`

- `GitHubPullRequest` -> `GiteaPullRequest`
  - `headRefName` -> `head.ref`
  - `headRefOid` -> `head.sha`
  - `baseRefName` -> `base.ref`
  - `state` 仅 open/closed，合并状态需看 `merged`

- `GitHubIssue` -> `GiteaIssue`
  - `issue.pull_request` 用于判定是否 PR
  - 无 `node_id`

---

**3. API 接口精确映射表 (API Endpoint Mapping)**

Gitea API Base Path：`/api/v1`

**3.1 GitHub GraphQL 查询 -> Gitea REST 组合**

| GitHub GraphQL 目的 | Gitea REST 替代 |
|---|---|
| PR 基础信息 | `GET /repos/{owner}/{repo}/pulls/{index}` |
| PR labels | 同 PR 基础信息，`labels` 数组 |
| PR commits | `GET /repos/{owner}/{repo}/pulls/{index}/commits` |
| PR files | `GET /repos/{owner}/{repo}/pulls/{index}/files` |
| PR reviews | `GET /repos/{owner}/{repo}/pulls/{index}/reviews` |
| PR review comments | `GET /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/comments` |
| Issue 基础信息 | `GET /repos/{owner}/{repo}/issues/{index}` |
| Issue/PR 通用评论 | `GET /repos/{owner}/{repo}/issues/{index}/comments` |
| 用户展示名 | `GET /users/{username}` |

**3.2 写操作映射**

| GitHub API | Gitea API |
|---|---|
| 创建 issue/PR 评论 | `POST /repos/{owner}/{repo}/issues/{index}/comments` |
| 获取/更新评论 | `GET/PATCH /repos/{owner}/{repo}/issues/comments/{id}` |
| 创建 PR review + inline comments | `POST /repos/{owner}/{repo}/pulls/{index}/reviews` |
| 创建分支 | `POST /repos/{owner}/{repo}/branches` |
| 删除分支 | `DELETE /repos/{owner}/{repo}/branches/{branch}` |
| 获取分支信息 | `GET /repos/{owner}/{repo}/branches/{branch}` |
| 文件新增/更新/删除 | `POST/PUT/DELETE /repos/{owner}/{repo}/contents/{filepath}` |

---

**4. Webhook Payload 与 Mock 数据重构规范**

**4.1 必须覆盖的事件（Gitea 1.21 实测）**
- `issues`
- `issue_comment`
- `pull_request`
- `pull_request_approved`（review 审批事件）
- `pull_request_comment`（review comment 事件）

说明：
- Gitea 1.21 实测不会发出 `pull_request_review` / `pull_request_review_comment`，而是使用上述两个事件名。

**4.2 Payload 差异关键点**

- GitHub comment 有 `node_id`，Gitea 没有
- Gitea `comment.id` 为纯数字
- Gitea review comment 只有 `position`，无 `line/start_line/side`
- PR `head/base` 路径结构不同
- GitHub 有 `installation`，Gitea 无
- `pull_request_approved` / `pull_request_comment` payload 中 **没有 `review.body`**，而是 `review.type` + `review.content`
- `pull_request_comment` payload **没有 `comment` 对象**（没有 `comment.id/path/position`），需要额外调用 `/pulls/{index}/reviews/{id}/comments` 才能拿到行内信息
- review 事件的 `action` 实测为 `reviewed`（而非 GitHub 的 `submitted/edited`）

**4.3 Mock Context 改造方案**

必须替换：

- `<REPO_ROOT>\test\mockContext.ts`
  - `IssueCommentEvent`、`PullRequestEvent` payload 改为 Gitea 结构
  - 删除所有 `node_id`、`databaseId`

**4.4 Fixture 更新清单**

- `<REPO_ROOT>\test\fixtures\sample-turns.json`
- `<REPO_ROOT>\test\fixtures\sample-turns-expected-output.md`
- `<REPO_ROOT>\test\fixtures\gitea-webhooks\issues-*.json`
- `<REPO_ROOT>\test\fixtures\gitea-webhooks\issue_comment-*.json`
- `<REPO_ROOT>\test\fixtures\gitea-webhooks\pull_request-*.json`
- `<REPO_ROOT>\test\fixtures\gitea-webhooks\pull_request_approved-*.json`
- `<REPO_ROOT>\test\fixtures\gitea-webhooks\pull_request_comment-*.json`

建议做法：
- 在 Gitea 实例创建真实 webhook，并保存 payload JSON 作为 fixture。

---

**5. System Prompt 与 Agent 认知重塑清单**

**5.1 必须修改的位置**
- `<REPO_ROOT>\src\create-prompt\index.ts`
- `<REPO_ROOT>\src\modes\tag\index.ts`
- `<REPO_ROOT>\src\modes\agent\index.ts`

**5.2 Prompt 必须加入的 Gitea 限制说明（示例）**

```text
<gitea_constraints>
- 你运行在 Gitea Actions (act_runner) 环境，不是 GitHub Actions。
- Gitea 不提供 GraphQL，请仅使用 Gitea REST API 或 MCP 工具。
- Gitea inline review comment 使用 diff position，不是 GitHub 的 line/side。
- 创建 PR 或评论请使用 Gitea API，而不是 GitHub compare URL。
</gitea_constraints>
```

**5.3 工具名替换**
- `mcp__github_comment__update_claude_comment` -> `mcp__gitea_comment__update_claude_comment`
- `mcp__github_file_ops__commit_files` -> `mcp__gitea_file_ops__commit_files`
- `mcp__github_inline_comment__create_inline_comment` -> `mcp__gitea_inline_comment__create_inline_comment`

---

**6. MCP Server 改造指导**

**6.1 文件操作服务器**
- `github-file-ops-server.ts` -> `gitea-file-ops-server.ts`
- GitHub Git Data API 在 Gitea 中不可用或不完整
- Gitea 推荐走 `contents` API，内容需 base64
- 多文件原子提交在 Gitea API 中难实现
- 若必须原子提交，建议改为 git CLI 路径

**6.2 评论服务器**
- `github-comment-server.ts` -> `gitea-comment-server.ts`
- 更新评论使用 `PATCH /issues/comments/{id}`

**6.3 Inline Comment 服务器**
- `github-inline-comment-server.ts` -> `gitea-inline-comment-server.ts`
- Gitea 必须通过 `POST /pulls/{index}/reviews` 创建
- 要求 `position`，需要 diff 映射
- 建议新增 diff->position 的计算逻辑
- 已实现 line/side -> position 映射：工具支持 `line` + `side` 或直接传入 `position`

**6.4 CI 服务器**
- Gitea OpenAPI 未暴露 Actions/Workflow APIs
- 建议先禁用该 MCP server，或改为读取日志文件/runner 输出

---

**7. GitHub 可用但 Gitea 有差异的文件清单**

以下文件在 GitHub 正常工作，但 Gitea 必须改：

说明：在新仓库 `<REPO_ROOT>` 中 `src/github` 目录应不存在；若仍残留，说明迁移未完成。替换实现统一位于 `src/gitea`。

- `<REPO_ROOT>\src\github\api\client.ts`
  - Octokit 替换为 Gitea client
- `<REPO_ROOT>\src\github\api\queries\github.ts`
  - GraphQL 全部删除
- `<REPO_ROOT>\src\github\context.ts`
  - GitHub Webhook payload 改 Gitea payload
- `<REPO_ROOT>\src\github\data\fetcher.ts`
  - GraphQL -> REST
- `<REPO_ROOT>\src\github\operations\comments\*`
  - 评论创建与更新 API 差异
- `<REPO_ROOT>\src\github\operations\branch.ts`
  - 使用 Gitea `/branches` API
- `<REPO_ROOT>\src\github\utils\image-downloader.ts`
  - GitHub `body_html` 与附件链接不可用
- `<REPO_ROOT>\src\mcp\github-*.ts`
  - 全部替换为 Gitea 版

---

**8. 分步实施任务清单（可直接拆为 Commit）**

- [x] 任务 1：复制仓库到新 Gitea 仓库并重写 `action.yml`
- [x] 任务 2：替换依赖（移除 Octokit / actions-toolkit）
- [x] 任务 3：新增 `src/gitea/api/client.ts` 并改造所有调用点
- [x] 任务 4：新增 `src/gitea/types.ts` 并替换引用
- [x] 任务 5：重写 `src/github/data/fetcher.ts` -> `src/gitea/data/fetcher.ts`
- [x] 任务 6：重写 comment/branch/file ops 相关逻辑
- [x] 任务 7：重写 MCP servers 为 Gitea 版本
- [x] 任务 8：更新 prompt 与 allowed tools 逻辑
- [x] 任务 9：更新测试与 fixture
- [x] 任务 10：更新 README 与迁移说明

**当前进度（截至 2026-03-11）**

- 已完成任务 1-3（Gitea Action 入口、依赖替换、Gitea client 与调用点改造）。
- 已提前完成部分任务 9：`test/mockContext.ts` 与 fixtures 已迁移为 Gitea 结构。
- 已接入 diff->position 计算：`src/gitea/utils/diff-position.ts` + `src/mcp/gitea-inline-comment-server.ts`（支持 line+side 或 position）。
- 已抓取真实 Gitea Webhook payload（issues/issue_comment/pull_request/pull_request_approved/pull_request_comment），并据此修正事件名与 review payload 结构。
- 已执行测试（2026-03-11）：`bun test` 通过（371 pass / 6 skip / 0 fail）。
  - 跳过项：Windows 环境下的 symlink 与 POSIX 权限测试（`test/github-file-ops-path-validation.test.ts`、`test/ssh-signing.test.ts`）。
  - Bun 版本：1.3.10。

---

**9. 补充要点（新增、容易被忽略但关键）**

- **环境变量兼容性**
  - Gitea Actions 是否提供 GitHub 等价的 `GITHUB_*` 环境变量必须验证。
  - 若缺失，需在 workflow 中显式设置，或引入兼容层。
- **分页与限流**
  - Gitea REST 端点默认分页，必须加 `page`/`limit` 参数，否则大 PR 会丢数据。
- **Diff Position 计算**
  - Gitea inline comment 依赖 `position`，必须使用 PR diff/patch 计算，不能直接用行号。
- **权限检测策略**
  - GitHub 的 `user.type` 无法在 Gitea 使用，需改用 `is_admin` 或 `allowed_bots` 白名单。
- **Step Summary 输出**
  - Gitea Actions 是否支持 `GITHUB_STEP_SUMMARY` 需要验证，必要时改为输出到 job 日志或 artifact。
- **Commit 签名**
  - Gitea API 不支持 GitHub 方式的多文件原子 commit，建议默认使用 git CLI 方式。
- **Webhook 事件命名差异**
  - Gitea 1.21 实测使用 `pull_request_approved` 与 `pull_request_comment`，review 只给 `review.type`/`review.content`，没有 `review.body` 或 `comment`。
  - 必须抓真实 payload 校准事件名与结构，否则触发检测会失败。
- **Webhook 回调白名单**
  - Docker 环境下 Gitea 默认禁止回调到内网地址，需在 `app.ini` 加：
    - `[webhook]`
    - `ALLOWED_HOST_LIST = *`
- **图片下载逻辑**
  - GitHub 的 `body_html`/user-attachments 在 Gitea 不可用，建议禁用或改为 `/markdown` 渲染。
- **base-action 依赖残留**
  - `base-action` 目录仍保留 `@actions/core` 及相关锁文件，若要完全去 GitHub 依赖需进一步清理。
- **本地开发依赖**
  - 需要预装 Bun 才能运行 `bun test` 与 `bun run typecheck`（当前已安装 1.3.10）。

---

**10. 推荐的最小可用实现路径**

- 第一阶段：先实现 Issue/PR 评论读写 + Prompt 生成 + git CLI 提交
- 第二阶段：补齐 inline comment、review、diff position
- 第三阶段：完善 CI、image 下载、扩展 webhook 事件

---

**完成判定标准**

- Tag 模式：能正确触发、生成 prompt、更新 comment、提交分支
- Agent 模式：能读取 prompt 并执行 CLI
- Gitea API 替换后，无 GitHub 专属依赖残留
- Mock/fixture 全部为 Gitea payload


